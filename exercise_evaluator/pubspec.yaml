name: exercise_evaluator
description: Aplicación móvil para evaluación de ejercicios en tiempo real

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.2

  # Cámara
  camera: ^0.10.5+2

  # HTTP requests
  http: ^1.1.0

  # Permisos
  permission_handler: ^11.0.1

  # Persistencia local (para futuras mejoras)
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
